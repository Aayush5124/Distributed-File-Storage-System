package com.example.project.node;

import java.util.Arrays;

public class NodeManager {
    // Example placeholder methods for managing nodes
    public void addNode(NodeInfo nodeInfo) {
        // Add node to the system
    }

    public NodeInfo getNodeInfo(String nodeId) {
        // Retrieve information about a node
        return new NodeInfo(nodeId, "localhost", 8080);
    }

    public void registernode(String node1, NodeInfo nodeInfo) {
        
    }

    public Arrays getAllNodes() {
    }
}